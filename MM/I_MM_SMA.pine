// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © pmoralesdev

//@version=6
indicator("I_MM_SMA", overlay = true, max_labels_count = 500)

//────────────────────────────────────────────//
// 📌 INPUTS                                  //
//────────────────────────────────────────────//
group_sl = "📌 Stop Loss"
slLookback = input.int(5, "Velas para calcular SL", minval = 1, group = group_sl)

group_sess = "📌 Horario operativa"
tradingSession = input.session("0800-2200", "Horario (HHMM-HHMM)", group = group_sess)

//────────────────────────────────────────────
// 📌 CÁLCULO DE MEDIAS
//────────────────────────────────────────────
smaEntry = ta.sma(close, 100)   
smaFast  = ta.sma(close, 25)    
smaSlow  = ta.sma(close, 50)   

plot(smaEntry, "SMA entrada (100)", color = color.blue, linewidth = 3)
plot(smaFast,  "SMA rápida (25)",   color = color.white,  linewidth = 1)
plot(smaSlow,  "SMA lenta (50)",    color = color.purple,   linewidth = 2)

// Estructura 25–50
bullStruct = smaFast > smaSlow
bearStruct = smaFast < smaSlow

//────────────────────────────────────────────
// 📌 TENDENCIA
//────────────────────────────────────────────
priceState = close > smaEntry ? 1 : close < smaEntry ? -1 : 0


//────────────────────────────────────────────
// 📌 HORARIO DE OPERATIVA
//────────────────────────────────────────────
inSession = not na(time(timeframe.period, tradingSession))

// Bar de apertura de sesión: pasa de fuera a dentro
sessionOpen = inSession and not inSession[1]
// Bar de cierre de sesión: pasa de dentro a fuera
sessionClose = not inSession and inSession[1]

//────────────────────────────────────────────
// 📌 CONDICIONES DE ENTRADA
//────────────────────────────────────────────
priceCrossUp   = ta.crossover(close, smaEntry)   // Precio cruza al alza la SMA100
priceCrossDown = ta.crossunder(close, smaEntry)  // Precio cruza a la baja la SMA100

// Stop Loss dinámico (últimas N velas)
slLong  = ta.lowest(low,  slLookback) 
slShort = ta.highest(high, slLookback)  

//TODO: Añadir filtro de volumen, velocidad, distancia...
enterLong  = priceCrossUp   and inSession and bullStruct
enterShort = priceCrossDown and inSession and bearStruct

//────────────────────────────────────────────
// 📌 CRUCES SMA25–SMA50
//────────────────────────────────────────────
smaCrossUp   = ta.crossover(smaFast, smaSlow)   // SMA25 cruza al alza SMA50
smaCrossDown = ta.crossunder(smaFast, smaSlow)  // SMA25 cruza a la baja SMA50

// Precio TP sugerido = close en el cruce SMA25–SMA50
tpPrice = close

//────────────────────────────────────────────
// 📌 LÍNEA DE SL GRÁFICA 
//────────────────────────────────────────────
var float slLine = na
var bool inLong  = false
var bool inShort = false

if enterLong
    slLine := slLong
    inLong  := true
else if enterShort
    slLine := slShort
    inShort := true

closeLongByCross  = inLong  and smaCrossDown and inSession
closeShortByCross = inShort and smaCrossUp   and inSession

closeSignal = (closeLongByCross or closeShortByCross)

if closeSignal
    slLine := na
else if closeLongByCross
    closeLongByCross := false
else if closeShortByCross
    closeShortByCross := false

if sessionClose
    slLine :=na
    closeLongByCross :=false
    closeShortByCross := false

//────────────────────────────────────────────
// 📌 ALERTAS
//────────────────────────────────────────────
activo = syminfo.ticker

// Mensajes entrada
longMsg  = "📈"  + activo + "\nST:" + str.tostring(slLong,  format.mintick)
shortMsg = "📉" + activo + "\nST:" + str.tostring(slShort, format.mintick)

// Mensaje cruce SMA25–SMA50 (posible TP/cierre)
closeMsg = "🎯" + activo + "\nTP:" + str.tostring(tpPrice, format.mintick)

// Mensaje de apertura de sesión: alcista / bajista según SMA100
string openMsg = ""
if close > smaEntry
    openMsg := "📈 " + activo + ": tendencia alcista"
else
    openMsg := "📉 " + activo + ": tendencia bajista"

// Mensaje de cierre de sesión (buenas noches)
string closeDayMsg = "Cierre de sesión " + activo + ": buenas noches"

// Envío de alertas
if enterLong
    alert(longMsg,  alert.freq_once_per_bar_close)

if enterShort
    alert(shortMsg, alert.freq_once_per_bar_close)

if closeSignal
    alert(closeMsg, alert.freq_once_per_bar_close)

if sessionOpen
    alert(openMsg, alert.freq_once_per_bar_close)

if sessionClose
    alert(closeDayMsg, alert.freq_once_per_bar_close)

//────────────────────────────────────────────
// 📌 SEÑALES VISUALES EN EL GRÁFICO
//────────────────────────────────────────────

// Flechitas en entradas
plotshape(enterLong, title="Entrada BUY", style=shape.triangleup, location=location.belowbar, color=color.lime, size=size.small, text="BUY")
plotshape(enterShort, title="Entrada SELL", style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, text="SELL")

// Línea horizontal del Stop Loss de la última señal
//plot(slLine, title="Stop Loss activo", color=color.red, linewidth=1, style=plot.style_linebr)

// Punto/cross en cada cruce SMA25–SMA50 (TP)
plot(closeSignal ? tpPrice : na, title="Cruce SMA25-50 (TP)", style=plot.style_cross, linewidth=5, color=color.yellow)
